# include <stdio.h>
int main(){
    int a,b;
    scanf("%d %d",&a,&b);
    if(a==b){
        printf("Both the elements are same :) ");
    }
    else {
        printf("Both elements are not same :( ");
    }

}