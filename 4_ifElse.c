#include <stdio.h>
int main(){
    int a,b;
    scanf("%d %d",&a,&b);
    if(a==b){
        printf("Both are same elements :) ");
    }
    else{
        printf("Both are different elements :( ");
    }
}